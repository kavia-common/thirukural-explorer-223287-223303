# Development environment configuration for CRA dev server
# Bind the dev server to all interfaces so preview URLs can reach it.
HOST=0.0.0.0

# Allow non-localhost preview hosts in development to avoid "Invalid Host header".
# IMPORTANT: This is only for local development environments.
DANGEROUSLY_DISABLE_HOST_CHECK=true

# If your preview environment needs HMR websocket hints, uncomment and set:
# WDS_SOCKET_HOST=your-preview-hostname
# WDS_SOCKET_PORT=3000

# Optional: Point API to a specific backend if not using package.json "proxy"
# REACT_APP_API_URL=https://your-backend-host:3001/api
